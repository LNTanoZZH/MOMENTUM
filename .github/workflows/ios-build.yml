name: iOS Build

on:
  push:
    branches: [main, master]
    paths-ignore:
      - 'ci-debug/**'
  pull_request:
    branches: [main, master]
  workflow_dispatch:

concurrency:
  group: ios-build-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build:
    name: Build MOMENTUM (Simulator)
    runs-on: macos-14
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode 15
        run: |
          if [ -d "/Applications/Xcode_15.4.app" ]; then
            sudo xcode-select -s /Applications/Xcode_15.4.app
          elif [ -d "/Applications/Xcode_15.2.app" ]; then
            sudo xcode-select -s /Applications/Xcode_15.2.app
          fi
          xcodebuild -version

      - name: List shared schemes
        run: xcodebuild -list -project MOMENTUM.xcodeproj

      - name: Build for iOS Simulator
        run: |
          set -o pipefail
          xcodebuild \
            -project MOMENTUM.xcodeproj \
            -scheme MOMENTUM \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            -configuration Debug \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            build 2>&1 | tee xcodebuild.log

      - name: Print compile errors
        if: failure()
        run: |
          echo "=== Compile errors ==="
          grep -E "error:|fatal error:" xcodebuild.log || true
          echo "=== Last 60 lines ==="
          tail -60 xcodebuild.log || true
          if [ -n "$GITHUB_STEP_SUMMARY" ]; then
            echo "## xcodebuild errors" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            grep -E "error:|fatal error:" xcodebuild.log >> "$GITHUB_STEP_SUMMARY" || echo "(no error lines found)" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Publish CI log to repository
        if: failure()
        run: |
          mkdir -p ci-debug
          cp xcodebuild.log ci-debug/last-xcodebuild.log
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ci-debug/last-xcodebuild.log
          git diff --staged --quiet && exit 0
          git commit -m "chore: update CI debug log [skip ci]"
          git push

      - name: Upload build log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: xcodebuild-log
          path: xcodebuild.log
          retention-days: 7
